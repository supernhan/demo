server:
  port: 8080
  servlet:
    context-path: /demo


spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/demo"
    username : root
    password : root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true